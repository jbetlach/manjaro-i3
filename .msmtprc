
# Betlach.name

account        betlach
host           smtp.1and1.com
protocol       smtp
auth           on
tls            on
# tls_starttls   off
tls_trust_file /etc/ssl/certs/ca-certificates.crt
port           587
from           honza@betlach.name
user           honza@betlach.name
password       
logfile        ~/.msmtp.log

# Set a default account

account default : betlach
